{
  "name": "notes",
  "type": "module",
  "private": true,
  "scripts": {
    "format": "bunx prettier . --write --log-level warn && terraform fmt -recursive",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "prettier": "3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
